services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: app-backend:latest
    container_name: app-backend
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - HOST=${HOST:-0.0.0.0}
      - PORT=${PORT:-8000}
    restart: unless-stopped
